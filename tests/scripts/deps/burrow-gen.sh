#!/usr/bin/env bash

# The git revision of Burrow we will build and install into ./bin/ for integration tests
dep status -f '{{if eq .ProjectRoot "github.com/hyperledger/burrow"}}#!/usr/bin/env bash

# Auto-generated by burrow-gen.sh, this is the version of Burrow from the Gopkg.lock
echo "{{.Revision}}"{{end}}'
